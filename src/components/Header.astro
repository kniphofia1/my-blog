---
import { SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
import ThemeToggle from './ThemeToggle.astro';
---

<header>
	<nav>
		<h2><a href="/">{SITE_TITLE}</a></h2>
		<div class="internal-links">
			<HeaderLink href="/">Home</HeaderLink>
			<HeaderLink href="/blog">Blog</HeaderLink>
			<HeaderLink href="/about">About</HeaderLink>
			<HeaderLink href="/search">搜索</HeaderLink>
		</div>
		<div class="nav-right">
			<ThemeToggle />
			<div class="social-links">
				<a href="https://github.com/kniphofia1" target="_blank">
					<span class="sr-only">访问 kniphofia1 的 GitHub</span>
					<svg viewBox="0 0 16 16" aria-hidden="true" width="32" height="32"
						><path
							fill="currentColor"
							d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.012 8.012 0 0 0 16 8c0-4.42-3.58-8-8-8z"
						></path></svg
					>
				</a>
				<a href="https://www.xiaohongshu.com/user/profile/66791f570000000003030f20" target="_blank">
					<span class="sr-only">访问 kniphofia1 的小红书</span>
					<svg viewBox="0 0 24 24" aria-hidden="true" width="32" height="32"
						><path
							fill="currentColor"
							d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm3-8c0 1.66-1.34 3-3 3s-3-1.34-3-3 1.34-3 3-3 3 1.34 3 3zm0 1.5c-1.78 0-5.33.89-5.33 2.67V16h10.67v-1.83c0-1.78-3.56-2.67-5.34-2.67z"
						></path></svg
					>
				</a>
			</div>
		</div>
	</nav>
</header>
<style>
	header {
		position: sticky;
		top: 0;
		z-index: 100;
		margin: 0;
		padding: var(--space-md) var(--space-xl);
		background: var(--glass-bg);
		backdrop-filter: var(--glass-blur);
		-webkit-backdrop-filter: var(--glass-blur);
		border-bottom: 1px solid var(--glass-border);
		box-shadow: var(--glass-shadow);
		transition: all var(--transition-base);
		animation: slideDown 0.5s ease-out;
	}

	/* Header 滑入动画 */
	@keyframes slideDown {
		from {
			opacity: 0;
			transform: translateY(-100%);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	[data-theme='dark'] header {
		background: rgba(17, 24, 39, 0.6);
		border-color: rgba(139, 92, 246, 0.2);
	}

	h2 {
		margin: 0;
		font-size: 1.25rem;
		font-weight: 700;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
		background: var(--gradient-primary);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		position: relative;
	}

	h2 a::after {
		content: '';
		position: absolute;
		bottom: -2px;
		left: 0;
		width: 100%;
		height: 2px;
		background: var(--gradient-primary);
		transform: scaleX(0);
		transform-origin: right;
		transition: transform var(--transition-base);
	}

	h2 a:hover::after {
		transform: scaleX(1);
		transform-origin: left;
	}

	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: var(--space-lg);
		max-width: 1400px;
		margin: 0 auto;
	}

	/* 内部导航链接 */
	.internal-links {
		display: flex;
		gap: var(--space-xs);
		align-items: center;
	}

	.internal-links a {
		padding: var(--space-sm) var(--space-md);
		color: var(--text-secondary);
		border-bottom: 2px solid transparent;
		text-decoration: none;
		font-size: 0.9375rem;
		font-weight: 500;
		transition: all var(--transition-fast);
		border-radius: var(--radius-sm);
	}

	.internal-links a:hover {
		color: var(--accent);
		background: var(--bg-tertiary);
	}

	.internal-links a.active {
		color: var(--accent);
		border-bottom-color: var(--accent);
	}

	/* 右侧区域 */
	.nav-right {
		display: flex;
		align-items: center;
		gap: var(--space-lg);
	}

	/* 社交链接 - 玻璃拟态 */
	.social-links {
		display: flex;
		gap: var(--space-md);
		align-items: center;
	}

	.social-links a {
		display: flex;
		align-items: center;
		justify-content: center;
		width: 44px;
		height: 44px;
		color: var(--text-secondary);
		border-radius: var(--radius-full);
		background: var(--glass-bg);
		backdrop-filter: var(--glass-blur);
		-webkit-backdrop-filter: var(--glass-blur);
		border: 1px solid var(--glass-border);
		transition: all var(--transition-base);
		position: relative;
		overflow: hidden;
		box-shadow: var(--shadow-sm);
	}

	.social-links a::before {
		content: '';
		position: absolute;
		inset: 0;
		background: var(--gradient-primary);
		opacity: 0;
		transition: opacity var(--transition-base);
	}

	.social-links a:hover {
		color: white;
		transform: translateY(-3px) rotate(5deg);
		box-shadow: var(--button-shadow-hover);
		border-color: transparent;
	}

	.social-links a:hover::before {
		opacity: 1;
	}

	.social-links a svg {
		position: relative;
		z-index: 1;
		width: 22px;
		height: 22px;
		transition: transform var(--transition-base);
	}

	.social-links a:hover svg {
		transform: scale(1.1);
	}

	/* 响应式设计 */
	@media (max-width: 768px) {
		header {
			padding: var(--space-sm) var(--space-md);
		}

		nav {
			flex-wrap: wrap;
			gap: var(--space-sm);
		}

		h2 {
			font-size: 1.125rem;
		}

		.internal-links {
			order: 1;
			flex: 1;
			gap: 0;
		}

		.internal-links a {
			padding: var(--space-xs) var(--space-sm);
			font-size: 0.875rem;
		}

		.nav-right {
			order: 2;
			gap: var(--space-sm);
		}

		.social-links {
			display: none;
		}
	}
</style>
